import { AdminLayout } from 'layouts'
// import { useSession } from 'next-auth/react'

const Client360Page = () => {
  // const { data: session } = useSession()
  // const { user } = session
  return <AdminLayout pageTitle="Client 360">Client 360</AdminLayout>
}

export default Client360Page
